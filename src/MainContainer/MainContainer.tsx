import "./MainContainer.css";

import * as React from "react";

const MainContainer: React.FunctionComponent = () => {
    return (
        <main>
            <h1 className="giveHeader">Give 2023 Loading.......</h1>
        </main>
    );
}
export default MainContainer;